#include<stdio.h>
void main(){
  int n,x,i,flag=0;
  scanf("%d %d\n",&n,&x);
  int a[n];
  for(i=0;i<n;i++){
      scanf("%d ",&a[i]);
  }
  for(i=0;i<n;i++){
      if(a[i]==x){
         printf("%d",i);
         flag=1;
      }
  }
  if(flag==0){
     printf("-1");
  }
}
